version: "3.3"

services:
    RabbitMQ:
      image: rabbitmq:3-management
      ports: 
        - "5672:5672"
        - "15672:15672"
      hostname: yt-rabbit
#      volumes:
#        - ./rabbit-db:/var/lib/rabbitmq